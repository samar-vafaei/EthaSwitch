#!KAMAILIO

# Define local variables
#
#!define LOCAL_IP "172.18.0.2" 
#!define LOCAL_PORT 5060
#!define ROUTING_TABLE '{"addresses":[{"ip_address":"172.18.0.7","port":5060}], "ani_number":"18098886368", "country_name":"Iran", "customer_ep":"routinspectore1", "customer_epg":"routinspector", "customer_id":"221", "customer_ip":"149.210.145.184", "customer_name":"RouteInspector","customer_rate":"0.10000000000000001", "destination_name":"Iran - Mobile MCI", "destination_number":"989120868654", "destination_prefix":"9891", "routing_table":{"routes":[{"ip_addresses":[{"ip_address":"172.18.0.5","port":5060},{"ip_address":"172.18.0.6","port":5060},{"ip_address":"172.18.0.7","port":5060}], "priority":"1", "provider_epg":"Odine_egr", "provider_id":"240", "provider_name":"Odine", "provider_rate":"0.01", "tech_prefix":"7676"}], "routing_model":"priority"}}'

####### Global Parameters #########

listen=udp:LOCAL_IP:LOCAL_PORT

### LOG Levels: 3=DBG, 2=INFO, 1=NOTICE, 0=WARN, -1=ERR
debug=2
log_stderror=no
 
memdbg=5
memlog=5
 
log_facility=LOG_LOCAL0
log_prefix="{$mt $hdr(CSeq) $ci} "
 
children=4
auto_aliases=no
 
server_header="Server: EthaSwitch"
user_agent_header="User-Agent: EthaLab"

####### Modules Section ########
 
/* set paths to location of modules */
#mpath="/usr/local/lib/kamailio/modules/"

loadmodule "kex.so"
loadmodule "corex.so"
loadmodule "tm.so"
loadmodule "tmx.so"
loadmodule "sl.so"
loadmodule "rr.so"
loadmodule "pv.so"
loadmodule "maxfwd.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"
loadmodule "siputils.so"
loadmodule "xlog.so"
loadmodule "debugger.so"
loadmodule "textops.so"
loadmodule "textopsx.so"
loadmodule "ipops.so"
loadmodule "avp.so"
loadmodule "jsonrpcs.so"
loadmodule "json.so"
loadmodule "jansson.so"
loadmodule "dialog.so"

loadmodule "htable.so"
modparam("htable", "htable", "a=>size=8;initval=0")
 
# auto-discard branches from previous serial forking leg
modparam("tm", "failure_reply_mode", 3)
# default retransmission timeout: 30sec
modparam("tm", "fr_timer", 30000)
# default invite retransmission timeout after 1xx: 120sec
modparam("tm", "fr_inv_timer", 120000)
modparam("tm", "auto_inv_100", 1)
modparam("tm", "reparse_invite", 1) 

# ----- rr params -----
# set next param to 1 to add value to ;lr param (helps with some UAs)
modparam("rr", "enable_full_lr", 0)
# do not append from tag to the RR (no need for this script)
modparam("rr", "append_fromtag", 0)

# ----- debugger params -----
modparam("debugger", "cfgtrace", 0)
modparam("debugger", "log_level_name", "exec")


####### Routing Logic ########
request_route {
	
	if(is_method("INVITE")){

	dlg_manage();
	
	$avp(rabbit_reply)=ROUTING_TABLE;

	#$avp(json_request) = "{\"b_number\":\""+$rU+"\", \"ip_address\":\""+$si+"\", \"port\":\""+$sp+"\", \"a_number\":\""+$fU+"\"}";
	#rabbitmq_publish_consume(RABBITMQ_EXCHANGE, RABBITMQ_ROUTING_KEY, "application/json", "$avp(json_request)", "$avp(rabbit_reply)");
	jansson_get("routing_table", "$avp(rabbit_reply)", "$avp(json_reply)");

	#$avp(json_reply)=ROUTING_TABLE;
	#xlog("^^^^ $avp(json_reply) ^^^^\r\n)");

	$var(i) = 0 ;
	jansson_array_size("routes", "$avp(json_reply)", "$avp(route_count)") ;
	if ( $avp(route_count) == 0){
		send_reply("404", "Not found");
		exit;
	}

	#$var(true)=1;
	while($var(i) < $avp(route_count)) {

		jansson_get("routes[$var(i)]", $avp(json_reply), "$avp(route)");
		jansson_get("tech_prefix", "$avp(route)", "$avp(destination_prefix)");

		$var(j) = 0; 
		jansson_array_size("ip_addresses", "$avp(route)", "$avp(ip_counts)");

		while($var(j) < $avp(ip_counts)){

		    jansson_get("ip_addresses[$var(j)].ip_address", "$avp(route)", "$avp(ip_address)");
		    jansson_get("ip_addresses[$var(j)].port", "$avp(route)", "$avp(port)");

		    if(!$avp(port)) {
			$avp(port)="5060";
		    }

		    if($avp(ip_address) != $null) {	

				#$var(ruri)=$avp(ip_address)+':'+$avp(port);
				#rewritehostporttrans("$var(ruri)");
				$rd=$avp(ip_address);
				$rp=$avp(port);

				t_on_reply("1");

				if(t_relay()){										
					xlog("**** ... $ru ... ******\r\n");					
				}
	           }
            
		   $var(j) = $var(j) + 1;
	        }

		$var(i) = $var(i) + 1;
	}
	}


	if(is_method("ACK|BYE")){
		xlog("+++++000 $sht(a=>$rd) ...  $sht(a=>$rp) 000+++++\r\n");
		#$rd=$sht(a=>$si);
		#$rp=$sht(a=>$sp);
		#if($dlg_var(ruri)){
		$rd=$dlg_var(ip_address);
		$rp=$dlg_var(port);
		xlog("+++++ $dlg_var(ip_address) ...  $dlg_var(port)  +++++\r\n");
		#}

		t_relay();
	}
	
	exit;
}

onreply_route[1] {
	if (t_check_status("(180)|(183)|2[0-9][0-9]")){
		xlog("#### $si ... $avp(ip_address) ####\r\n");
		$sht(a=>$si)=$si;
		$sht(a=>$sp)=$sp;
		$dlg_var(ip_address)=$avp(ip_address);
		$dlg_var(port)=$avp(port);

		$var(i)=$avp(route_count);
		$var(j)=$avp(ip_counts);
	}
}

event_route[dialog:start] {
		$dlg_var(ip_address)=$avp(ip_address);
		$dlg_var(port)=$avp(port);
        $sht(a=>$rd)=$dlg_var(ip_address);
        $sht(a=>$rp)=$dlg_var(port);		
}

