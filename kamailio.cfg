#!KAMAILIO

# Define local variables
#!define LOCAL_IP "172.18.0.2" 
#!define LOCAL_PORT 5060

####### Global Parameters #########
listen=udp:LOCAL_IP:LOCAL_PORT

### LOG Levels: 3=DBG, 2=INFO, 1=NOTICE, 0=WARN, -1=ERR
debug=2
log_stderror=no
 
memdbg=5
memlog=5
 
log_facility=LOG_LOCAL0
log_prefix="{$mt $hdr(CSeq) $ci} "
 
children=4
auto_aliases=no

shm_mem_size=800
server_header="Server: EthaSwitch"
user_agent_header="User-Agent: EthaLab"

####### Modules Section ########
/* set paths to location of modules */
#mpath="/usr/local/lib/kamailio/modules/"

loadmodule "kex.so"
loadmodule "corex.so"
loadmodule "tm.so"
loadmodule "tmx.so"
loadmodule "sl.so"
loadmodule "rr.so"
loadmodule "pv.so"
loadmodule "maxfwd.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"
loadmodule "siputils.so"
loadmodule "xlog.so"
loadmodule "debugger.so"
loadmodule "textops.so"
loadmodule "textopsx.so"
loadmodule "ipops.so"
loadmodule "avp.so"
loadmodule "dialog.so"

loadmodule "db_postgres.so"

loadmodule "keepalive.so"
modparam("keepalive", "destination", "172.18.0.5")
modparam("keepalive", "destination", "172.18.0.6")
modparam("keepalive", "destination", "172.18.0.7")

loadmodule "drouting.so"
modparam("drouting", "db_url","postgres://kamailio:kamailiorw@172.18.0.8/kamailio")
modparam("drouting", "enable_keepalive", 1)
 
# auto-discard branches from previous serial forking leg
modparam("tm", "failure_reply_mode", 3)
# default retransmission timeout: 30sec
modparam("tm", "fr_timer", 30000)
# default invite retransmission timeout after 1xx: 120sec
modparam("tm", "fr_inv_timer", 120000)
modparam("tm", "auto_inv_100", 1)
modparam("tm", "reparse_invite", 1) 

# ----- rr params -----
# set next param to 1 to add value to ;lr param (helps with some UAs)
modparam("rr", "enable_full_lr", 0)
# do not append from tag to the RR (no need for this script)
modparam("rr", "append_fromtag", 0)

# ----- debugger params -----
modparam("debugger", "cfgtrace", 0)
modparam("debugger", "log_level_name", "exec")


####### Routing Logic ########
request_route {

	if(is_method("INVITE")){
		do_routing();
		t_on_failure("serial");
	}

	if(is_method("ACK|BYE")){
		loose_route();
	}

	t_relay();
	exit;
}

failure_route["serial"]{
	if(!next_routing()){
		exit;
	}

	if(next_routing()){

		t_on_failure("serial");

		t_relay();
		exit;
	}
}




