#!KAMAILIO

# Define local variables
#!define LOCAL_IP "172.18.0.2" 
#!define LOCAL_PORT 5060

####### Global Parameters #########
listen=udp:LOCAL_IP:LOCAL_PORT

### LOG Levels: 3=DBG, 2=INFO, 1=NOTICE, 0=WARN, -1=ERR
debug=2
log_stderror=no
 
memdbg=5
memlog=5
 
log_facility=LOG_LOCAL0
log_prefix="{$mt $hdr(CSeq) $ci} "
 
children=4
auto_aliases=no

shm_mem_size=800
server_header="Server: EthaSwitch"
user_agent_header="User-Agent: EthaLab"

####### Modules Section ########
/* set paths to location of modules */
#mpath="/usr/local/lib/kamailio/modules/"

loadmodule "kex.so"
loadmodule "corex.so"
loadmodule "tm.so"
loadmodule "tmx.so"
loadmodule "sl.so"
loadmodule "rr.so"
loadmodule "pv.so"
loadmodule "maxfwd.so"
loadmodule "usrloc.so"
loadmodule "registrar.so"
loadmodule "siputils.so"
loadmodule "xlog.so"
loadmodule "debugger.so"
loadmodule "textops.so"
loadmodule "textopsx.so"
loadmodule "ipops.so"
loadmodule "avp.so"
loadmodule "dialog.so"

loadmodule "ctl.so"
modparam("ctl", "binrpc", "udp:172.18.0.2:3000") 
# modparam("ctl", "binrpc", "unix:/var/run/kamailio/kamailio_ctl")

loadmodule "jsonrpcs.so"
modparam("jsonrpcs", "transport", 2)
modparam("jsonrpcs", "fifo_name", "/tmp/kamailio_jsonrpc.fifo")

loadmodule "db_text.so"
modparam("db_text", "db_mode", 1)
modparam("db_text", "db_delim", "|")
modparam("db_text", "default_connection", "text:///etc/kamailio/dbtext")

loadmodule "htable.so"
modparam("htable", "htable", "ha_re_grp=>size=10;autoexpire=7200;dbtable=re_grp;cols='reg_exp,group_id';")
modparam("htable", "htable", "ha_dispatcher=>size=10;autoexpire=7200;dbtable=dispatcher;cols='setid,destination,flags,priority,attrs';")
modparam("htable", "db_url", "text:///etc/kamailio/dbtext")

# auto-discard branches from previous serial forking leg
modparam("tm", "failure_reply_mode", 3)
# default retransmission timeout: 30sec
modparam("tm", "fr_timer", 30000)
# default invite retransmission timeout after 1xx: 120sec
modparam("tm", "fr_inv_timer", 120000)
modparam("tm", "auto_inv_100", 1)
modparam("tm", "reparse_invite", 1) 

# group module
loadmodule "group.so"
modparam("group", "db_url","text:///etc/kamailio/dbtext")
modparam("group", "re_table", "re_grp")

# DISPATCHER module
loadmodule "dispatcher.so"
modparam("dispatcher", "db_url", "text:///etc/kamailio/dbtext")
modparam("dispatcher", "flags",2) 
#modparam("dispatcher", "use_default", 1) --> choose last destination in XAVP as a default route
modparam("dispatcher", "xavp_dst", "_dsdst_")
modparam("dispatcher", "ds_ping_method", "OPTIONS")
modparam("dispatcher", "ds_ping_interval", 15)
modparam("dispatcher", "ds_probing_mode", 1)
modparam("dispatcher", "ds_hash_size", 10)
modparam("dispatcher", "ds_timer_mode", 1)

# ----- rr params -----
# set next param to 1 to add value to ;lr param (helps with some UAs)
modparam("rr", "enable_full_lr", 0)
# do not append from tag to the RR (no need for this script)
modparam("rr", "append_fromtag", 0)

# ----- debugger params -----
modparam("debugger", "cfgtrace", 0)
modparam("debugger", "log_level_name", "exec")


####### Routing Logic ########
request_route {

	# populate re_grp table: 76149 76046 2123 12415 123 7676 2000100 
	# (tech_prefix(incoming SIP trunk DID number) + destintion_pefix)
	if(is_method("INVITE")){
		dlg_manage();		
		
		if(get_user_group("Request-URI", "$avp(s:setid)")){	

			if(ds_select_routes("$avp(setid)=10;$avp(setid)=11", "ruri")) {

				$var(prefix)=$(xavp(_dsdst_=>attrs){s.select,2,;}{s.select,1,=});
				$var(strip)=$(xavp(_dsdst_=>attrs){s.select,3,;}{s.select,1,=});
				
				$rU=$var(prefix)+$(rU{s.substr,$(var(strip){s.int}),0}); 
				$dlg_var(rU)=$rU;			

				t_on_failure("REROUTE");
				route(RELAY);
			}
		}

		t_on_reply("1");
	}

	if(is_method("ACK|BYE")){	

		if(is_method("BYE")){	
			ds_load_update();
		}

		$rU=$dlg_var(rU); 	
		$rd=$dlg_var(ruri);

		route(RELAY);
	}
}

route[RELAY]{
	t_relay();
	exit;
}

failure_route["REROUTE"]{
	if(t_check_status("3[0-9][0-9]|4[0-9][0-9]|5[0-9][0-9]")){
		if(ds_next_domain()){
			t_on_failure("REROUTE");
			route(RELAY);
		}		
	}
}

event_route[dialog:start]{	
	$dlg_var(ruri)=$si;
}

onreply_route[1]{
    
	if(is_method("INVITE"))
	{
		if(t_check_status("2[0-9][0-9]"))
			ds_load_update();
		if(t_check_status("3[0-9][0-9]|4[0-9][0-9]|5[0-9][0-9]"))
			ds_load_unset();
    }
}
