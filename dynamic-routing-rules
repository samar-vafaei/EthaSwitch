
    start_time: $Ts ---> INVITE
    alert_time: $Ts ---> 180 Ringing
    connect_time: $Ts ---> event[dialog:start] (200 OK)
    disconnect_time: $Ts ---> event[dialog:stop] (BYE)
    destination_name: $tU

    # left-hand side
    # before do-routing()
    customer:
        call_id: $dlg(callid)
        name
        id
        epgroup
        destination_prefix
        signalling_ip
        country_name
        destination_name
        ani_number: $fU
        destination_number
        disconnect_cause: $rs

    # right-hand side
    # after do-routing()
    provider:
        call_id: $ci
        signalling_ip: $
        name
        id
        epgroup
        tech_prefix
        rate
        ani_number: $fU
        destination_number
        disconnect_cause: $rs



dr_rules table ---> dr_rules
dr_gateways table ---> provider
dr_groups table ---> customer 

        
















        







